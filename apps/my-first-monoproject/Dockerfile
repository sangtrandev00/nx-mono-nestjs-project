# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build my-first-monoproject`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t my-first-monoproject`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system my-first-monoproject && \
          adduser --system -G my-first-monoproject my-first-monoproject

COPY dist/apps/my-first-monoproject my-first-monoproject/
RUN chown -R my-first-monoproject:my-first-monoproject .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix my-first-monoproject --omit=dev -f install

CMD [ "node", "my-first-monoproject" ]
